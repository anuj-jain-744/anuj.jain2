import { TravelInsuranceType } from './ConstantValue';

describe('ConstantValue', () => {
  it('should have the correct value for TravelInsuranceType', () => {
    expect(TravelInsuranceType).toBe('Travel Insurance');
  });
});
